<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- ============================================================
             PORTAL USERS: Can only read their own applications
             base.group_portal = external users who log in
             domain_force filters records at database level
             ============================================================ -->
        <record id="job_application_portal_rule" model="ir.rule">
            <field name="name">Portal: own job applications only</field>
            <field name="model_id" ref="model_quality_job_application"/>
            <!-- partner_id matches the logged-in portal user's partner -->
            <field name="domain_force">[('partner_id', '=', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="0"/>
            <field name="perm_create" eval="0"/>
            <field name="perm_unlink" eval="0"/>
        </record>

        <!-- ============================================================
             INTERNAL USERS: Can read/write/create all applications
             base.group_user = employees / internal staff
             [(1,'=',1)] = match everything (no filter)
             ============================================================ -->
        <record id="job_application_internal_rule" model="ir.rule">
            <field name="name">Internal: all job applications</field>
            <field name="model_id" ref="model_quality_job_application"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_create" eval="1"/>
            <field name="perm_unlink" eval="1"/>
        </record>
    </data>
</odoo>
